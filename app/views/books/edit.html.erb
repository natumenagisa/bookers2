<main>
  <%= render 'error', model: @book %>
  <div class="container">
    <div class="row">
      <div class="col-ms12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
        <h1>Editing Book</h1>
        <%= render 'books/newbook', book: @book %>
        <%= link_to "Show", book_path(@book) %>
        |
        <%= link_to "Back", books_path %>
      </div>
    </div>
  </div>
</main>